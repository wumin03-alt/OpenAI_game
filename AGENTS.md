# Codex 작업 규칙

이 저장소에서 코드, Unity 씬, 프리팹, UI 또는 아트를 변경하는 모든 작업자는 작업 전에 다음 문서를 순서대로 읽어야 한다.

1. `GAME_SPEC.md`
2. `TECH_SPEC.md`
3. `UI_ART_GUIDE.md`
4. `OWNERSHIP.md`

## 필수 원칙

- Unity 프로젝트 루트는 저장소 루트가 아니라 `RPG DEMO/`이다.
- 담당 영역 밖의 씬, 프리팹, 공통 인터페이스는 수정하지 않는다.
- 공용 기능은 새 싱글턴이나 매니저를 만들지 말고 `TECH_SPEC.md`의 기존 계약을 사용한다.
- 계약 변경이 꼭 필요하면 구현을 중단하고 변경 이유, 영향 파일, 마이그레이션 방법을 먼저 보고한다.
- 작업 시작 전에 `git status --short --branch`로 브랜치와 기존 변경을 확인한다. 다른 사람의 변경은 정리하거나 커밋하지 않는다.
- Unity가 자동 변경한 `Packages/`, `ProjectSettings/`, 렌더 파이프라인 설정은 담당자 승인 없이 커밋하지 않는다.
- Unity 씬과 프리팹은 같은 파일을 여러 사람이 동시에 수정하지 않는다. 소유자가 아닌 사람은 이벤트 또는 공개 API로 연결한다.
- 새 Unity 에셋은 대응하는 `.meta` 파일과 함께 관리한다.
- `main`에 직접 푸시하지 않는다. 본인의 `dev/*` 브랜치에서 검증 후 통합한다.

## 완료 보고 형식

모든 작업은 다음 다섯 항목을 빠짐없이 보고해야 한다.

1. 변경한 파일
2. 발생시키거나 구독하는 이벤트
3. 다른 시스템이 연결할 방법
4. Unity에서 테스트하는 방법
5. 아직 연결되지 않은 부분

오류 없이 컴파일되지 않거나 플레이 흐름을 직접 확인하지 못했다면 완료라고 표현하지 말고 검증 범위와 이유를 함께 적는다.
