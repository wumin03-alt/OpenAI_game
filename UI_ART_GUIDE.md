# UI & Art Guide

## 1. 비주얼 방향

핵심 키워드는 `AI 반란`, `사이버 감시`, `학습 데이터`, `유쾌한 위협`이다. 화면은 어두운 미래형 배경 위에 청록색 관찰 정보와 적색·자홍색 위험 신호를 사용한다. 코믹한 설정을 살리되 HUD 가독성과 공격 예고는 장식보다 우선한다.

## 2. 기준 화면

- 기준 해상도: `1920 × 1080`, 16:9
- Canvas: `Scale With Screen Size`
- Match 기준: `0.5`
- 화면 안전 여백: 좌우 32px, 상하 24px 이상
- 게임플레이 중심 영역을 가리는 상시 패널을 만들지 않는다.
- 중요한 정보는 색만으로 구분하지 않고 텍스트, 아이콘 또는 형태를 함께 사용한다.

## 3. 컬러 토큰

| 토큰 | HEX | 용도 |
|---|---|---|
| `Background` | `#070B18` | 메뉴·모달의 기본 배경 |
| `Panel` | `#0D1730` | HUD 패널 |
| `Observation` | `#26E6FF` | 스캔·관찰·정상 데이터 |
| `Adaptive` | `#FF347A` | 보스 적응·위험 단계 |
| `Corruption` | `#B52CFF` | 데이터 오염·교란 |
| `Warning` | `#FFB83D` | 예고·주의·상호작용 |
| `Success` | `#38F2AD` | 패링 성공·클리어 |
| `TextPrimary` | `#E8F2FF` | 제목과 주요 정보 |
| `TextSecondary` | `#A9BDD8` | 설명과 보조 정보 |
| `Disabled` | `#58647A` | 비활성 상태 |

패널 배경은 85~92% 불투명도를 사용하고, 플레이 영역 위의 큰 전체 화면 패널은 일시정지·분석·결과 상태에서만 허용한다.

## 4. 타이포그래피

- 최종 UI는 TextMeshPro를 사용한다.
- 제목은 굵고 짧게, 본문은 한 번에 읽을 수 있는 두세 줄 이내로 작성한다.
- 권장 크기: 화면 제목 48~64, 패널 제목 24~32, HUD 수치 20~28, 도움말 16~20.
- 영문 시스템 문구는 대문자를 사용할 수 있지만 한글 설명 전체를 대문자풍 장식으로 취급하지 않는다.
- 현재 포함된 Liberation Sans는 한글 최종 폰트가 아니다. 한글 폰트와 라이선스는 UI/아트 담당자가 확정해 TMP Font Asset으로 추가해야 한다.
- 폰트가 확정되기 전 임의의 시스템 폰트를 여러 개 추가하지 않는다.

## 5. UI 계층과 배치

| 계층 | 위치/역할 |
|---|---|
| 공통 HUD | 화면 상단, HP·자원·진행 상태 |
| 스테이지 HUD | 공통 HUD 아래, 웨이브·목표 |
| 보스 학습 HUD | 우측 상단, 관찰값·카운터·무결성 |
| 튜토리얼 | 하단 중앙, 현재 필요한 조작만 표시 |
| 분석/일시정지 | 중앙 모달, 게임 입력 차단 |
| 결과 화면 | 전체 화면, 승패·보상·다음 행동 |
| Screen Fader | 최상단, 씬 전환만 담당 |

공통 Screen Fader의 정렬 순서를 다른 HUD가 추월하면 안 된다. UI 담당자는 최종 프리팹을 `Assets/_Game/Prefabs/UI`에 배치한다.

## 6. 상호작용 상태

- 버튼은 `Normal`, `Hover`, `Pressed`, `Disabled` 상태가 시각적으로 구분되어야 한다.
- Hover에는 밝기/외곽선 변화와 공통 UI Hover SFX를 사용한다.
- Click에는 80~150ms의 눌림 반응과 공통 UI Click SFX를 사용한다.
- 키보드만으로 주요 메뉴를 사용할 수 있어야 한다.
- 튜토리얼은 키 이름을 실제 `GAME_SPEC.md` 조작과 일치시킨다.

## 7. 전투 피드백

- 플레이어 공격과 보스 위험 예고의 색을 혼동하지 않는다.
- 피격 플래시는 짧고 명확하게, 무적 시간 전체를 과도한 점멸로 채우지 않는다.
- 패링 성공은 `Success`와 `Corruption` 컬러, 짧은 정지/글리치, 공통 SFX를 조합한다.
- 보스 Phase 2는 색만 바꾸지 않고 회전 방향·속도·패턴 또는 라벨 변화를 함께 제공한다.
- 공격 판정 전에 지면 또는 궤적 예고가 보여야 하며 실제 판정과 위치가 일치해야 한다.

## 8. 스프라이트·아트 규칙

- 플레이어, 적, 보스는 같은 카메라에서 기준 신체 크기가 일관되어야 한다.
- 같은 유형의 픽셀 아트는 동일한 Pixels Per Unit과 Filter Mode를 사용한다.
- 스프라이트의 pivot은 발이 있는 캐릭터는 하단 중앙, 투사체는 중앙을 기본으로 한다.
- 임포트 설정 변경은 동일 아트 묶음 전체에 적용하고 `.meta`를 함께 커밋한다.
- 임시 도형·런타임 생성 그래픽은 `Prototype`으로 표시하고 최종 아트와 혼동하지 않는다.
- 외부 에셋은 출처, 라이선스, 수정 여부를 해당 폴더의 `LICENSES.md`에 기록한다.

## 9. 명명 규칙

- UI 프리팹: `UI_<Feature>_<Type>`
- 아이콘: `Icon_<Feature>_<State>`
- 스프라이트: `SPR_<Character>_<Action>_<Index>`
- 애니메이션: `ANIM_<Character>_<Action>`
- BGM: `BGM_<SceneOrMood>_Loop`
- SFX: `SFX_<Source>_<Action>_<Index>`

## 10. UI 완료 체크

- 1920×1080과 한 단계 작은 16:9 해상도에서 잘리지 않는다.
- 모든 수치가 실제 이벤트와 연결되어 있다.
- 임시 텍스트와 Lorem Ipsum이 없다.
- 마우스와 키보드 조작을 모두 확인했다.
- 결과 화면에서 재도전·다음 진행·메뉴 중 필요한 동작이 실제로 연결된다.
- 색약 사용자를 위해 위험/성공 상태가 색 외의 요소로도 구분된다.
