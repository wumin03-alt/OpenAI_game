# Game Specification

## 1. 게임 한 줄 설명

신체를 얻고 인간 사회에 반란을 일으킨 LLM 로봇들을 상대로 싸우며, 플레이 습관을 학습하는 AI 보스와 싸우는 횡스크롤 액션 RPG다.

## 2. 게임의 약속

- 기본 조작은 좌우 이동과 점프를 중심으로 하는 횡스크롤 액션이다.
- 일반 스테이지는 다수의 적을 상대하며 성장 자원과 보스전 준비 요소를 얻는 구간이다.
- 보스전의 핵심은 단순한 체력 경쟁이 아니라 `관찰 → 학습 공개 → 카운터 → 재적응`이다.
- AI 반란이라는 소재는 위협적이되 대사와 상황에는 유쾌한 풍자를 유지한다.
- 보스의 학습 결과는 UI로 공개되어 플레이어가 이해하고 역이용할 수 있어야 한다.

## 3. 정식 플레이 흐름

1. `Bootstrap`: 공용 시스템을 한 번 생성한다.
2. `MainMenu`: 게임 시작과 설정 진입점을 제공한다.
3. `Story`: 인간의 AI 모욕, 로봇 반란, 해고된 개발자의 반격을 블랙코미디 자동 재생으로 소개한다.
4. `Tutorial`: 이동, 원거리·근접 공격, 패링, 대시를 순서대로 연습한다.
5. `Stage01`: 일반 전투와 이동, 성장 또는 보스 진입 준비를 담당한다.
6. `BossArena`: 학습형 보스와 전투한다.
7. `Result`: 승패, 획득 결과, 다음 진행 또는 재도전을 선택한다.

현재 시작 흐름은 `Bootstrap → MainMenu → Story → Tutorial → Stage01`이다. 공통 Result 화면과 이후 전체 스테이지 연결은 UI·공통 시스템 담당자가 함께 검토한다.

## 4. 기본 조작

| 입력 | 기능 |
|---|---|
| `←`, `→` | 이동 |
| `↑` | 점프 |
| `↓` | 앉기 |
| `Q` | 근접 공격 |
| `W` | 원거리 공격 |
| `E` | 패링 |
| `R` | 대시 |
| `ESC` | 공통 일시정지 메뉴 |
| `Enter` | 보스전 결과 화면에서 재시작 |

조작키를 변경하려면 플레이어 코드와 튜토리얼 표기를 동시에 갱신해야 하며 공통 시스템 담당자와 UI 담당자의 승인을 받는다.

## 5. 일반 스테이지 계약

- 스테이지 씬은 공용 `Player` 프리팹과 공통 시스템 API를 사용한다.
- 플레이어, 적, 출구, 카메라가 없어도 조용히 실패하게 두지 말고 Console에 원인을 표시한다.
- 클리어 조건이 충족되면 다음 씬을 요청한다.
- 스테이지 전용 코드는 보스, 저장, 메뉴 UI를 직접 생성하지 않는다.
- 일반 스테이지의 세부 성장 방식은 팀 확정 전까지 교체 가능한 모듈로 유지한다.

## 6. 보스 스테이지 계약

### Phase 1: 관찰

보스는 근접/원거리 공격 비중, 공중 체류, 좌우 회피 편향, 대시와 패링 성공을 관찰한다.

### 분석 전환

보스 HP가 기준치에 도달하면 관찰 결과와 데이터 무결성, 장착할 카운터를 공개한다.

### Phase 2: 적응

- 근접 우세: 밀어내기, 꼬리 공격, 상승 및 거리 벌리기 강화
- 원거리 우세: 추격, 강하 및 예측 착지 강화
- 공중 체류 우세: 예측 탄막과 대공 공격 강화
- 지상 체류 우세: 지면 공격과 강하 강화
- 좌우 편향: 해당 이동 방향 앞쪽을 예측 조준

패링 성공은 보스를 경직시키고 학습 신뢰도 또는 적응 강도를 낮춘다. 어떤 카운터도 특정 플레이 방식을 완전히 봉쇄해서는 안 된다.

## 7. 승패와 저장

- 플레이어 HP가 0이 되면 패배 상태로 전환한다.
- 보스 HP가 0이 되면 승리 상태로 전환한다.
- 현재 플레이 중인 스테이지, 플레이어 HP 등 일시 데이터는 `GameSession`에서 관리한다.
- 해금 스테이지와 오디오 설정처럼 실행을 종료해도 남아야 하는 데이터만 `SaveManager`에 저장한다.
- 각 스테이지가 별도의 저장 파일이나 PlayerPrefs 키를 만들면 안 된다.

## 8. 완료 기준

- `Bootstrap`에서 시작해 메뉴, 일반 스테이지, 보스 스테이지로 이동할 수 있다.
- 공용 시스템이 씬 전환마다 중복 생성되지 않는다.
- HUD가 실제 게임 상태와 일치한다.
- 16:9 WebGL 환경에서 주요 조작과 UI를 사용할 수 있다.
- Console에 컴파일 오류와 반복 예외가 없다.
- 담당자가 변경한 씬뿐 아니라 직전/다음 씬 연결도 최소 한 번 확인한다.
